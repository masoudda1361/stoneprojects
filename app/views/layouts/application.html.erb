<!DOCTYPE html>
<html>
  <head lang="<%= I18n.locale %>">
    <title><%= t 'global.menu.title1' %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  </head>

  <body>

      <nav class="navbar navbar-expand-sm navbar-light bg-light round display: flex">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><%= t 'global.menu.Brand' %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active px-4" aria-current="page" href="/"><%= t 'global.menu.home' %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active px-4" href="/product/"><%= t 'global.menu.product' %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active px-4" href="/contact/"><%= t 'global.menu.contact' %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true"></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>





<ul class="list-group list-group-horizontal">
  <% I18n.available_locales.each do |locale| %>
    <li class="list-group-item">
      <% if I18n.locale == locale %>
        <%= t(locale, scope: 'locales') %>
      <% else %>
        <%= link_to t(locale, scope: 'locales'), url_for(locale: locale) %>
      <% end %>
    </li>
  <% end %>
</ul>

    <%= yield %>
  </body>
</html>
